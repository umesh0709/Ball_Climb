using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResetLogRotation : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    { 
      //  xMove = CrossPlatformInputManager.GetAxis("Horizontal");
        //print(xMove);
        //xOffset = xMove * sideForce * Time.deltaTime;
        //newPos = xOffset + transform.localPosition.x;

        //transform.localPosition = new Vector3(newPos, transform.localPosition.y, transform.localPosition.z);
    }
}
